# import knapsack_reader import read_knapsack_probelm

# def n():
    # list = []
    
    # # f_list = [1 if x == 0 else 0 for x in list]
    # # return f_list

    # f_list = []
    # for i in range(len(list)):
        # f_list.append(1 if list[i] == 0 else 0)
    # return f_list


# for i in range(len(list)):
    # list.append()

# result = n()
# print(result)



# import knapsack_reader
# import read_knapsack_probelm

def n(s):
    # Define the list
    my_list = [0, 1, 1, 0, 0]
    
    # Iterate through the list using range and len
    for i in range(len(my_list)):
        print(f"Index {i}: {my_list[i]}")

    # Alternatively, you can iterate directly through elements
    for element in my_list:
        print(element)