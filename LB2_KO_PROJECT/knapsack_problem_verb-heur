# import knapsack_reader import read_knapsack_probelm

def n():
    list = []
    
    # f_list = [1 if x == 0 else 0 for x in list]
    # return f_list

    f_list = []
    for i in range(len(list)):
        f_list.append(1 if list[i] == 0 else 0)
    return f_list


for i in range(len(list)):
    list.append()

result = n()
print(result)